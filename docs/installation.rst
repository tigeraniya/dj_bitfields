============
Installation
============

At the command line::

    $ easy_install dj_bitfields

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv dj_bitfields
    $ pip install dj_bitfields
